# AI客服插件系统 - 安装和运行指南

## 🚀 快速开始

### 第一步：安装依赖

```bash
# 进入项目目录
cd h:\douyin\实战项目

# 安装依赖
pip install -r requirements.txt
```

### 第二步：配置环境

1. 复制环境配置文件：
```bash
copy .env.example .env
```

2. 编辑 `.env` 文件，配置API密钥：
```
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 第三步：启动服务

```bash
# 方式1：使用启动脚本
python start.py

# 方式2：直接运行后端
python backend/main.py

# 方式3：使用uvicorn
uvicorn backend.main:app --reload --host localhost --port 8000
```

### 第四步：访问系统

打开浏览器访问：http://localhost:8000

## 📋 系统流程演示

### 客户端流程
1. 打开网页，自动连接AI客服
2. 输入问题，AI客服自动响应
3. 如果不满意，点击"转人工客服"按钮
4. 系统自动创建工单，等待客服接入

### 客服端流程
1. 查看待处理工单：GET /api/tickets
2. 接入工单：POST /api/tickets/accept
3. 开始人工服务
4. 关闭工单：POST /api/tickets/close

## 🔧 API测试

### 测试AI客服
```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test123","message":"你好"}'
```

### 测试转人工
```bash
curl -X POST http://localhost:8000/api/request-human \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test123","customer_id":"customer_test123","reason":"测试转人工"}'
```

### 查看工单列表
```bash
curl http://localhost:8000/api/tickets
```

### 接入工单
```bash
curl -X POST http://localhost:8000/api/tickets/accept \
  -H "Content-Type: application/json" \
  -d '{"ticket_id":"工单ID","agent_id":"客服001"}'
```

## 🎨 插件扩展

### 添加新插件

1. 创建插件文件：`backend/plugins/my_plugin.py`

```python
from backend.plugin_interface import PluginInterface, PluginInfo

class MyPlugin(PluginInterface):
    def initialize(self, config):
        return True
    
    def execute(self, input_data):
        return {"result": "success"}
    
    def shutdown(self):
        return True
    
    def get_info(self):
        return PluginInfo(
            name="my_plugin",
            version="1.0.0",
            description="自定义插件"
        )
```

2. 注册插件：在 `backend/main.py` 中添加

```python
from backend.plugins.my_plugin import MyPlugin

my_plugin = MyPlugin()
plugin_manager.register_plugin("my_plugin", my_plugin, {})
plugin_manager.initialize_plugin("my_plugin")
```

## 🐛 常见问题

### 1. 依赖安装失败
- 检查Python版本（需要3.11+）
- 使用国内镜像源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 2. API密钥无效
- 检查 `.env` 文件配置
- 确认API密钥格式正确
- 使用国内模型API（DeepSeek等）

### 3. 端口占用
- 修改 `.env` 中的 `SERVER_PORT`
- 或使用命令指定端口：`uvicorn backend.main:app --port 8001`

### 4. 前端页面无法访问
- 检查 `frontend` 目录是否存在
- 确认 `index.html` 文件路径正确

## 📊 性能优化

### 1. 使用缓存
- 缓存AI响应结果
- 缓存常见问题答案

### 2. 异步处理
- 使用异步API调用
- 并发处理多个请求

### 3. 数据库优化
- 使用索引优化查询
- 定期清理历史数据

## 🔐 安全建议

### 1. API密钥保护
- 不要在代码中硬编码API密钥
- 使用环境变量存储敏感信息

### 2. 输入验证
- 验证客户输入内容
- 防止恶意输入攻击

### 3. 访问控制
- 限制API访问频率
- 添加身份验证机制

## 📈 监控和日志

### 查看日志
系统日志会输出到控制台，包含：
- 插件初始化状态
- API请求日志
- 错误和异常信息

### 性能监控
访问 `/api/status` 查看系统状态：
```bash
curl http://localhost:8000/api/status
```

## 🚀 生产部署

### Docker部署
```bash
# 构建镜像
docker build -t ai-customer-service .

# 运行容器
docker run -p 8000:8000 ai-customer-service
```

### 云部署
- 使用云服务器部署
- 配置域名和SSL证书
- 设置负载均衡

## 📞 技术支持

如有问题，请查看：
- 项目文档：README.md
- 插件架构：插件架构说明.md
- API文档：访问 http://localhost:8000/docs